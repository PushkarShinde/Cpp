#include<iostream>
using namespace std;

int main() {

    int a = 2;
    cout << (a << 1);
    // Binary Representation of 2 is 10 after that, one bit of left shift operation is now binary is 100 which is equal to 4.
    // similarly, '>>' operator will do right shift.
}
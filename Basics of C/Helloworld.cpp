#include <iostream>  // This is the header file that includes input-output stream
using namespace std;  // This allows direct access to std elements without needing std::

int main() {
    cout << "Hello, World!" << endl;  // Prints Hello, World! followed by a new line
    return 0;  // Indicates that the program ended successfully
}
